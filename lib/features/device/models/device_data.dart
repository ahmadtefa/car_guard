import 'package:flutter/foundation.dart';

/// Immutable placeholder model for the device status contract.
///
/// The firmware contract has not been finalized yet, so this model remains
/// intentionally empty and does not introduce any guessed fields.
@immutable
class DeviceStatus {
  /// Creates an immutable placeholder device status model.
  const DeviceStatus();
}

/// Immutable placeholder model for sensor data.
@immutable
class SensorData {
  /// Creates an immutable placeholder sensor data model.
  const SensorData();
}

/// Immutable placeholder model for battery data.
@immutable
class BatteryData {
  /// Creates an immutable placeholder battery data model.
  const BatteryData();
}

/// Immutable placeholder model for temperature data.
@immutable
class TemperatureData {
  /// Creates an immutable placeholder temperature data model.
  const TemperatureData();
}

/// Immutable placeholder model for coolant level data.
@immutable
class CoolantLevelData {
  /// Creates an immutable placeholder coolant level data model.
  const CoolantLevelData();
}
